<h1>Listing compare_models</h1>
<%= link_to 'New Compare model', new_compare_model_path %>

<table>
  <tr>
    <th>Model</th>
    <th>Name</th>
    <th>Website</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @compare_models.each do |compare_model| %>
  <tr>
    <td><%= compare_model.model_id %></td>
    <td><%= compare_model.name %></td>
    <td><%= compare_model.website_id %></td>
    <td><%= compare_model.value %></td>
    <td><%= link_to 'Show', compare_model_path(compare_model) %></td>
    <td><%= link_to 'Edit', edit_compare_model_path(compare_model) %></td>
    <td><%= link_to 'Destroy', compare_model_path(compare_model), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

